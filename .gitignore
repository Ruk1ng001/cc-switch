# ── 渠道 / 密钥真实值 ─────────────────────────────────────
# 含 token，绝不进 git。线上由 CI Secret 注入，本地由 *.env 提供。
# 只跟踪 *.env.example（示例，无真实值）。
*.env
!*.env.example
auth.json
*.pem
*.key
id_rsa*

# ── 本地覆盖 / 构建产物 ───────────────────────────────────
*.local
/dist/
/target/

# ── agent 运行时凭据 ──────────────────────────────────────
.claude/
.chief/
